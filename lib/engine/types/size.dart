/// A class representing a two-dimensional size with width and height.
/// 
/// The [Size] class is used to define the dimensions of an rectangular object.
/// Both [width] and [height] are required and must be provided during
/// instantiation.
class Size {

  final double width;
  final double height;
  
  Size({required this.width, required this.height});

}